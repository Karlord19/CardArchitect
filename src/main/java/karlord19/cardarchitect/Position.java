package karlord19.cardarchitect;

class Position {
    int x, y;
    public String toString() {
        return "pos: (" + x + ", " + y + ")";
    }
    Position(int x, int y) {
        this.x = x;
        this.y = y;
    }
}
